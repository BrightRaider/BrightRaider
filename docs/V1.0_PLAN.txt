================================================================================
V1.0 — PLANUNGSDOKUMENT
================================================================================

V1.0 ist kein normales Feature-Update — es ist ein kompletter Überarbeitung.
Ziel: stabiles, poliertes Produkt das bereit ist für ein breiteres Publikum.
Signalisiert "fertige Software" stärker als V9.x

Vorherige Versionsbezeichnung: V10 → umbenannt zu V1.0 (SemVer ab jetzt)
Alte Tags (v6–v9.x) bleiben auf GitHub unverändert.


KERNZIEL: VIBRANCE GUI ERSETZEN
================================

Nutzer die aktuell VibranceGUI + BrightRaider parallel laufen haben,
sollen danach nur noch BrightRaider brauchen.

Klarer Hinweis in der App: "VibranceGUI nicht mehr nötig."


FEATURES
========

[ ] Alt-Tab Vibrance Auto-Switch [FREE]
    - WinEvent-Hook auf Foreground-Window-Change (SetWinEventHook)
    - Spiel im Fokus → Profil-Vibrance aktiv
    - Alt-Tab / anderes Fenster → normale Vibrance (gespeicherter Hardware-Wert)
    - Ersetzt VibranceGUI vollständig — universell, nicht Arc Raiders-spezifisch
    - Konfigurierbar: welches Fenster gilt als "Spiel" (Prozessname)
    - Klarer Hinweis in der App: "VibranceGUI nicht mehr nötig"
    - Selber Hook: Map Overlay + Crosshair automatisch ausblenden wenn Spiel nicht
      im Vordergrund (Alt-Tab raus → weg, Alt-Tab rein → wieder da)

[ ] Arc Raiders Modus — opt-in
    - Beim ersten Start: nur universelle Features sichtbar
      (Helligkeit, Vibrance, Alt-Tab Auto-Switch, Profile, Autorun, Audio)
    - Haken "Arc Raiders features aktivieren" → Map Scanner, QuickSelect,
      Evac Alarm tauchen im Menü auf
    - Macht das Menü für Nicht-Arc-Raiders-Nutzer sauber und übersichtlich
    - Arc Raiders-Nutzer die upgraden: Haken ist bereits gesetzt (Config-Migration)

[ ] Redesigned Menus
    - Aktuelles Tray-Menü ist gewachsen und unübersichtlich
    - Cleaner, besser organisiert, intuitiver
    - Einstellungen logisch gruppiert
    - Durch Arc Raiders opt-in automatisch schlanker für alle anderen

[ ] Bessere Usability & In-App-Guidance
    - Neue Nutzer sollen ohne Anleitung zurechtkommen
    - Tooltips / Hinweise wo nötig
    - Erster Start: kurze Einführung oder Setup-Assistent

[ ] Tray-Icon zeigt Profilname statt Nummer
    - Statt "2" steht im Icon "AR" oder ein frei definierbares Kürzel
    - Klein, schnell gebaut, sofort sichtbar besser

[ ] Hue im Profil-Editor (Issue #39, dawuus)
    - Vierte Achse neben Gamma/Contrast/Vibrance: Hue 0–359°
    - NVIDIA: NvAPI_SetHue / NvAPI_GetHue
    - AMD: ADL_Display_Color_Set mit ColorType=Hue
    - Use Case: regional zensiertes Blut sichtbar machen (grün → rot)
    - Restore on Exit (originalHue wie originalVibrance)
    - Config: profiles[i].Hue, default 0
    - UI: Slider/Spinner im Profil-Editor neben Vibrance
    - Aufwand: ~1 Tag, passt zu Settings-Overhaul

[ ] QuickSelect: Maus-für-alle-Slots (Issue #35, spix18 — AZERTY)
    - Aktuell nutzen Slots 1–6 PostMessage-Tastendruck → bricht auf AZERTY
      (und wahrscheinlich anderen Layouts mit Shift-Top-Row)
    - Slots 7–10 nutzen SetCursorPos → funktioniert universell
    - Lösung: SetCursorPos für ALLE Slots, Tastatur-Pfad komplett raus
    - Erfordert: Wheel-Position-Konstanten für Slots 1–6 vermessen
      (1 Uhr, 2 Uhr, 3 Uhr, 4 Uhr, 5 Uhr, 6 Uhr — relativ zur Bildschirmmitte)
    - Vorhandene Konstanten als Vorlage: QS_SLOT7_DX/DY (6 Uhr)
      bis QS_SLOT10_DX/DY (10–11 Uhr)
    - Bonus: Layout-unabhängig, robuster gegen Anti-Cheat-Filter,
      ein Code-Pfad statt zwei

[ ] Breiteres Publikum
    - Messaging ändert sich: nicht mehr "BrightRaider für Arc Raiders"
      sondern "BrightRaider für jeden der Digital Vibrance nutzt —
      mit Arc Raiders Pro-Features on top"
    - Marketing auf r/pcgaming, r/gaming, allgemeine Gaming-Foren möglich
    - VibranceGUI hat tausende Nutzer — das ist die Zielgruppe


STRATEGIE — SPIELUNTERSTÜTZUNG
================================

Universelle Features (Vibrance, Helligkeit, Alt-Tab Auto-Switch) skalieren
ohne Mehraufwand auf jedes Spiel — kein Wartungsaufwand pro Spiel.

Tiefe Integration (Map Scanner, QuickSelect, Evac Alarm) bleibt exklusiv
für Arc Raiders. Gründe:
- Jedes neue Spiel bedeutet: Koordinaten einmessen, testen, bei jedem
  Patch nachpflegen, Support für fremde Auflösungen
- Arc Raiders wird selbst gespielt → eigene Qualitätskontrolle möglich
- Bei fremden Spielen: abhängig von Community-Feedback, kein eigenes Testen

Kein zweites Spiel mit tiefer Integration bis:
- Arc Raiders Potenzial vollständig ausgeschöpft ist
  (56k aktive Spieler, 116k Spitze — erst ein Bruchteil erreicht)
- jemand aus der Community aktiv hilft (Koordinaten einmessen, testen)

Preis: Ein Pro für alles — kein Modul-System, kein separater Checkout.
Arc Raiders opt-in ist UI-Entscheidung, kein separates Produkt.


LANGZEIT-VISION — BRIGHTRAIDER ALS UNIVERSELLES GAMER-TOOL
===========================================================

Ziel: BrightRaider wird nicht Arc-Raiders-Tool mit Extras, sondern das
mächtigste portable Gamer-Tool für Windows — für jedes Spiel.

BrightRaiders größter Vorteil muss erhalten bleiben:
- Eine EXE, keine Installation, keine Dependencies
- Läuft überall, funktioniert im Fullscreen, kein Alt-Tab
- Kein Installer, kein Store, kein Bloat

Zielarchitektur: Plugin-System
- Kern-EXE bleibt klein, universell, portable
- Spiel-Module als separate .dll Dateien die nachgeladen werden
- Kern wartet: Vibrance, Helligkeit, Alt-Tab Auto-Switch, Profile
- Module liefern: Spiel-spezifische Features (Map Scanner, QuickSelect etc.)
- Community kann eigene Module bauen für ihre Spiele
- Du wartest nur den Kern — Spiele skalieren ohne deinen Aufwand

Was NICHT funktioniert:
- .NET Solution mit Installer → verliert "läuft überall"
- Electron/Web → viel zu schwer für ein Tray-Tool
- Store-App → Anti-Cheat blockiert Low-Level Hooks

Wann umsteigen:
- Nicht jetzt — V1.0 zuerst, Nutzerbasis aufbauen
- Wenn Community aktiv Spiel-Module nachfragt
- Wenn eine .cs Datei zu klein für den Umfang wird

Vorbereitung in V1.0 — skalierbar bauen ohne zu overengineeren:
- Arc Raiders Features sauber hinter arcRaidersMode-Flag isolieren
  → das passiert sowieso für den opt-in
  → ist bereits die logische Trennung die ein Plugin-System später braucht
  → kein Mehraufwand, zahlt direkt auf V1.0 ein
- Noch NICHT bauen: echtes .dll Plugin-System, Plugin-API, Loader
  → Wochen Arbeit für Infrastruktur die noch niemand braucht
  → erst wenn klar ist wie Module wirklich aussehen sollen
- Faustregel: Kern und Spiel-Features klar trennen im Code,
  aber keine Plugin-Infrastruktur bis die Nachfrage da ist


CONFUSEREX — ABGESCHAFFT
=========================

ConfuserEx wird nicht mehr verwendet (abgeschafft ab V9.6).
Die periodische Online-Lizenzvalidierung (alle 3 Tage, silent) ist die eigentliche
Schutzschicht. ConfuserEx erhöhte AV-Detektionen ohne echten Sicherheitsmehrwert.

Build-Script kompiliert direkt mit Roslyn, kein ConfuserEx-Schritt mehr.
XOR-Verschleierung der Seeds im Code bleibt bestehen.


CRYPTO-SEEDS — KEINE ÄNDERUNG BEI V1.0
========================================

Seeds bleiben unverändert (seit V9.3 neu, kein Leak seitdem).
Online-Validierung macht Seeds als Angriffspunkt irrelevant:
korrekte Seeds + gefälschte .lic → wird online abgelehnt.
Neue Seeds = unnötiger Re-Aktivierungs-Aufwand ohne Sicherheitsgewinn.

Neue Seeds nur bei konkretem Sicherheitsproblem (nachgewiesener Leak).


TECHNISCHE HINWEISE
===================

SetWinEventHook für Alt-Tab Detection:
- EVENT_SYSTEM_FOREGROUND (0x0003) → feuert wenn Vordergrundfenster wechselt
- Callback prüft ob neues Fenster == Spielprozess
- Kein Polling nötig, sehr niedrige CPU-Last
- Funktioniert mit allen Anti-Cheat-Systemen (nur Windows-API)

================================================================================


CODE REVIEW — TECHNICAL DEBT FÜR V1.0
========================================

Befunde aus dem Review von src/v9.6/BrightRaider.cs (5956 Zeilen).
Die KRITISCHEN Punkte für Issue #36 wurden bereits in V9.6.1 als Hotfix
adressiert. Die folgenden sind für V1.0 vorgesehen — größere Refactorings
oder Härtungen, die in einem Hotfix nicht reinpassen.


[ ] HookCallback aufräumen — aktuell 335 Zeilen
    - Marshal.PtrToStructure wird mehrfach pro Event allokiert → einmal parsen
    - Logik in kleinere Dispatcher-Methoden auslagern
    - Magic Numbers (0x6E, 0x10u, 0x101, 0x6F, …) als const extrahieren
    - Profile-Hotkey-Code für Numpad 1-9 + TopRow 1-9 ist 9× dupliziert →
      Lookup-Tabelle (VK → ProfileIndex), 5 Zeilen statt 60+

[ ] Race Conditions absichern
    - ocrScanRunning: Hook-Thread und Background-Thread schreiben/lesen ohne
      volatile/Interlocked. Zwei Scans könnten parallel starten.
      Fix: Interlocked.CompareExchange.
    - *LongPressTimer-Pattern (M-Key, Mute, Autorun): nicht atomares
      "if(t!=null) { x=null; t.Dispose(); }" — bei sehr schnellem Down/Up
      doppelter Dispose möglich. Fix: Interlocked.Exchange(ref timer, null).

[ ] OCR Temp-File loswerden
    - OcrFromBitmap schreibt aktuell fixe Datei %TEMP%\br_ocr_tmp.png
      (5–8× pro Scan). Zwei Scans parallel → File-Race.
    - Andere User auf der Maschine können den Game-Screenshot mitlesen
      (World-Readable Temp).
    - Fix: WinRT direkt aus MemoryStream + RandomAccessStream.CreateFromStream,
      keine Disk-IO.

[ ] QuickSelect-Datenstruktur aufräumen
    - 8 parallele Arrays (qsKey1..8, qsSlot1..8, qsAction1..8Timer/H/Q/Use/Active)
    - Refactor zu QuickSelectSlot Class oder Struct[] — Config-Load/Save wird
      30 Zeilen kürzer und Hinzufügen eines neuen Felds wird trivial.
    - Gleiches Pattern für QuickSave-Slots (5 parallele Arrays).

[ ] Icon-Handle-Leak in MakeIcon
    - Icon.FromHandle(hIcon) ohne Dispose, nur .Clone() →
      original GDI-Handle wird nicht freigegeben, akkumuliert bis GC.
    - Fix: using (var tmp = Icon.FromHandle(hIcon)) return (Icon)tmp.Clone();

[ ] Auto-Brightness MeasureZone optimieren
    - Aktuell 256 GetPixel-Calls pro Zone × 5 Zonen = 1280 GDI-Roundtrips/Tick.
    - 16×16 Bitmap macht das tolerabel, aber LockBits + Pointer-Read wäre
      ~50× schneller und weniger DWM-Last.

[ ] License-Online-Check härten
    - Aktuell naiver Substring-Match auf "valid":true. Ein MITM mit
      gefälschtem JSON ({"x":"valid":true,...}) würde durchkommen.
    - Fix: echtes JSON-Parsing (System.Web.Script.Serialization oder
      einfacher Regex auf "valid"\s*:\s*true).
    - Optional: Cert-Pinning via ServerCertificateValidationCallback mit
      Pinned Public-Key-Hash (Lemon Squeezy hat fixen Cert-Issuer).

[ ] WinRTWait Timeout
    - Blockiert Thread mit mre.Wait() ohne Timeout → bei OCR-Engine-Hang
      hängt der Scan-Thread ewig.
    - Fix: mre.Wait(5000) + Cancel-Path.

[ ] BreakReminder Validierung
    - breakReminderTimer.Interval = breakReminderIntervalMin * 60 * 1000
      bei sehr großen Werten (> 35791 Min) Overflow auf int.
    - Fix: clamp auf 5..240 wie in der UI-Validierung.

[ ] Datei-Aufteilung erwägen
    - 5956 Zeilen in einer .cs ist mit Roslyn-Kompile schnell, aber
      wartbar nicht mehr. Bei V1.0-Migration auf .NET 8 / Native AOT (siehe oben)
      ist sowieso eine .csproj nötig — dann gleich nach Bereichen splitten:
      Hooks/, OCR/, License/, Crosshair/, Audio/, QuickSelect/, QuickSave/,
      Display/.

================================================================================
